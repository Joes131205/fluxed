import React from "react";
import { Text, View } from "react-native";

export const Navbar = () => {
    return (
        <View className="w-full">
            <Text>Navbar</Text>
        </View>
    );
};
